---
title: "Wallet Statement"
description: "Get transaction history for a managed wallet"
---



Generate a **transaction history** for a specific wallet over a given period.

## Endpoint

<Badge variant="success">**GET**</Badge> `/pms/wallets/{wallet_id}/statement`

## Path Parameters

<ParamField path="wallet_id" type="string" required>
  Unique ID of the wallet.
</ParamField>

## Query Parameters

<ParamField query="from" type="string">
  Start date in ISO 8601 format (e.g. `2024-01-01`).
</ParamField>

<ParamField query="to" type="string">
  End date in ISO 8601 format (e.g. `2024-01-31`).
</ParamField>

## Headers

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `Authorization` | string | **Required** | Bearer token with your secret API key |

## Response

<ResponseField name="status" type="string">
  Request status.
</ResponseField>

<ResponseField name="data" type="array">
  <Expandable title="Transaction Item">
    <ResponseField name="id" type="string">
      Transaction ID.
    </ResponseField>
    <ResponseField name="amount" type="string">
      Amount moved.
    </ResponseField>
    <ResponseField name="type" type="string">
      `credit` or `debit`.
    </ResponseField>
    <ResponseField name="description" type="string">
      Narration or reason.
    </ResponseField>
    <ResponseField name="created_at" type="string">
      Timestamp.
    </ResponseField>
  </Expandable>
</ResponseField>

## Example Request

```bash
curl -X GET "https://api.payvessel.com/pms/wallets/WLT_123456/statement?from=2024-02-01&to=2024-02-29" \
  -H "Authorization: Bearer sk_test_..."
```

